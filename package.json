{
  "name": "zvault",
  "private": true,
  "workspaces": [
    "sdk",
    "frontend"
  ],
  "scripts": {
    "build:sdk": "cd sdk && bun run build",
    "build:frontend": "cd frontend && bun run build",
    "build": "bun run build:sdk && bun run build:frontend",
    "dev": "cd frontend && bun run dev"
  }
}
