{
  "name": "zvault",
  "private": true,
  "workspaces": [
    "sdk",
    "zvault_app"
  ],
  "scripts": {
    "build:sdk": "cd sdk && bun run build",
    "build:app": "cd zvault_app && bun run build",
    "build": "bun run build:app",
    "dev": "cd zvault_app && bun run dev",
    "dev:sdk": "cd sdk && bun run build && cd ../zvault_app && bun run dev"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "bech32": "^2.0.0"
  }
}
