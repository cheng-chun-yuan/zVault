{
  "name": "zvault-contracts",
  "version": "0.1.0",
  "description": "zVault Solana smart contracts (Pinocchio)",
  "scripts": {
    "build": "cargo build-sbf",
    "deploy": "solana program deploy target/deploy/zvault_pinocchio.so",
    "deploy:devnet": "solana program deploy target/deploy/zvault_pinocchio.so -u devnet",
    "deploy:localnet": "solana program deploy target/deploy/zvault_pinocchio.so -u localhost",
    "test": "bun run tests/zvault.ts",
    "test:mocha": "npx ts-mocha -p ./tsconfig.json -t 120000 tests/**/*.ts",
    "test:demo": "bun run scripts/test-demo-flow.ts",
    "test:name": "bun run scripts/test-name-registry.ts",
    "test:all": "bun run scripts/test-all.ts",
    "setup:devnet": "bun run scripts/devnet-setup.ts"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.98.4",
    "@zvault/sdk": "file:../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "react": "^19.0.0",
    "typescript": "^5.0.0"
  }
}
