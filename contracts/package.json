{
  "name": "zvault-contracts",
  "version": "0.1.0",
  "description": "zVault Solana smart contracts (Pinocchio)",
  "scripts": {
    "build": "cargo build-sbf",
    "deploy": "solana program deploy target/deploy/zvault_pinocchio.so",
    "test": "bun run tests/zvault.ts",
    "setup:devnet": "bun run scripts/devnet-setup.ts",
    "init": "bun run scripts/deploy-and-seed.ts"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
