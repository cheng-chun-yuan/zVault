{
  "name": "zvault-noir-circuits",
  "version": "1.0.0",
  "description": "Noir circuits for zVault with proof generation and testing",
  "type": "module",
  "scripts": {
    "compile": "bun run compile:all",
    "compile:all": "bun run compile:claim && bun run compile:transfer && bun run compile:split && bun run compile:partial_withdraw && bun run compile:pool_deposit && bun run compile:pool_withdraw && bun run compile:pool_claim_yield",
    "compile:claim": "cd claim && nargo compile",
    "compile:transfer": "cd transfer && nargo compile",
    "compile:split": "cd split && nargo compile",
    "compile:partial_withdraw": "cd partial_withdraw && nargo compile",
    "compile:pool_deposit": "cd pool_deposit && nargo compile",
    "compile:pool_withdraw": "cd pool_withdraw && nargo compile",
    "compile:pool_claim_yield": "cd pool_claim_yield && nargo compile",
    "test": "bun run test:noir && bun run test:js",
    "test:noir": "bun run test:claim && bun run test:transfer && bun run test:split && bun run test:partial_withdraw && bun run test:pool_deposit && bun run test:pool_withdraw && bun run test:pool_claim_yield",
    "test:claim": "cd claim && nargo test",
    "test:transfer": "cd transfer && nargo test",
    "test:split": "cd split && nargo test",
    "test:partial_withdraw": "cd partial_withdraw && nargo test",
    "test:pool_deposit": "cd pool_deposit && nargo test",
    "test:pool_withdraw": "cd pool_withdraw && nargo test",
    "test:pool_claim_yield": "cd pool_claim_yield && nargo test",
    "test:js": "bun test",
    "prove": "bun run scripts/prove.ts",
    "generate-inputs": "bun run scripts/generate-inputs.ts"
  },
  "dependencies": {
    "@aztec/bb.js": "3.0.3",
    "@msgpack/msgpack": "^3.1.3",
    "@noir-lang/noir_js": "1.0.0-beta.18"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
