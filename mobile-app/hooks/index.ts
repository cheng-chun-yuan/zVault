/**
 * Custom Hooks
 *
 * Export all custom hooks for the mobile app.
 */

export { useNameRegistry, type ResolvedRecipient } from './useNameRegistry';
export { useDeposit, type DepositResult } from './useDeposit';
