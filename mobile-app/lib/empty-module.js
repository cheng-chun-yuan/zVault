/**
 * Empty module placeholder for WASM dependencies
 *
 * React Native uses native noir-react-native instead of WASM.
 * This prevents bundling errors from import.meta usage.
 */
module.exports = {};
