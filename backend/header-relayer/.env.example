# Solana RPC endpoint
SOLANA_RPC_URL=https://api.devnet.solana.com

# BTC Light Client program ID
PROGRAM_ID=S6rgPjCeBhkYBejWyDR1zzU3sYCMob36LAf8tjwj8pn

# Relayer keypair (JSON array of bytes)
# Generate with: solana-keygen new --outfile relayer.json
# Then copy the content here as a JSON array
RELAYER_KEYPAIR=[50,128,114,...]

# Polling interval in milliseconds (default: 30 seconds)
POLL_INTERVAL_MS=30000

# Bitcoin network: mainnet, testnet, or signet
BITCOIN_NETWORK=testnet

# Starting block height (REQUIRED)
# Set this to a recent block height to avoid syncing from genesis
# Check current height: curl https://mempool.space/testnet/api/blocks/tip/height
START_BLOCK_HEIGHT=2900000
